import { css } from "@emotion/react";
import Color from "@src/common/styles/Color";

export default {
  wrapper: css`
    position: fixed;
    right: 2%;
    bottom: 5%;
    z-index: 1;
    cursor: pointer;
  `,
};
